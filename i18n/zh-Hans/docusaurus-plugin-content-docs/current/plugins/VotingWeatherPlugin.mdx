---
description: 让玩家按指定间隔对服务器天气进行投票
---

import CodeBlock from '@theme/CodeBlock';
import cfg from "!!raw-loader!./reference/plugin_voting_weather_cfg.reference.yml";

# VotingWeatherPlugin
该插件让玩家按指定的时间间隔对服务器天气进行投票。

::::note

要想获得平滑的天气过渡和降雨效果，你需要在 `extra_cfg.yml` 中将 `EnableWeatherFx` 设为 `true`。

::::

## 配置
在 `extra_cfg.yml` 中启用该插件
```yaml title="extra_cfg.yml"
EnablePlugins:
- VotingWeatherPlugin
```

### 参考配置
关于可用于 `BlacklistedWeathers` 的天气类型列表，请参阅 [WeatherFX 天气类型](../misc/wfx-types.md)
<CodeBlock language="yaml" title="plugin_voting_weather_cfg.yml">{cfg}</CodeBlock>
